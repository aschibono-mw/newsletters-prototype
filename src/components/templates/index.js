/**
 * Template Components
 *
 * Shared components that demonstrate how DS primitives compose
 * into common UI patterns used across template pages.
 *
 * These are NOT a separate component library - they're examples
 * of applying the design system to real product patterns.
 */

export { default as TemplateHeader } from './TemplateHeader'
export { default as SearchFilterBar } from './SearchFilterBar'
export { default as MetricsCard, MetricsGrid } from './MetricsCard'
